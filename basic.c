#include<stdio.h>
int main()
    {
        int i;
        int arr[5]={10,20,30,40,50};   //declaring and initialising the array

        for(i=0;i<5;i++)  // transversing an array
        printf("value of arr[%d] is %d\n",i,arr[i]);

    }